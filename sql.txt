-- DS: r_ds_iraction_general
-- Check if a specific test OID exists as a test action
SELECT *
FROM irac_test_action
WHERE program_id = '17024'      -- Quest campaign ID
  AND oid = '<TEST_OID_HERE>';


-- DS: iraction_sharded
-- DRAFT – based on vetted OID/campaign lookup query
SELECT action_id,
       oid,
       campaign_id,
       ircm_terms_id,
       payout_trace,
       sku,
       ircm_category_list_id,
       doe
FROM irac_action2
WHERE campaign_id = 17024
  AND oid = '<TEST_OID_HERE>'
ORDER BY doe;


-- DS: r_ds_ods
-- DRAFT – adapt to Quest
SELECT *
FROM ir_api_log
WHERE iram_account_id = 3596860            -- Quest billing account ID
  AND ircm_campaign_id = 17024             -- Program ID
  AND doe BETWEEN '2025-11-03' AND '2025-11-10'
  -- Optional: uncomment when you have specifics:
  -- AND ircm_tracker_id = <TRACKER_ID>
  -- AND oid = '<HASHED_OID_HERE>'
LIMIT 200;
