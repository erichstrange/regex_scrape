-- vetted query: finance_billing_subscription_lines_by_ba_id
SELECT subscription_line_id,
       status,
       currency,
       start_date,
       end_date
FROM   finance.subscription_line
WHERE  billing_account_id = 6209213;

