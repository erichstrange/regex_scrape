SELECT
  ircm_campaign_id,
  tracking_domain,
  tracking_domain_type,
  imp_track_domain,
  base_site_domain,
  permitted_deeplink_domains,
  dlu,
  ulu
FROM ircm_technicalintegration
WHERE ircm_campaign_id IN (27291);


SELECT *
FROM irad_ad
WHERE ircm_campaign_id = 27291;


SELECT *
FROM ircm_blockredirect
WHERE ircm_campaign_id IN (27291);
-- Optionally narrow with:
-- AND rule_json ILIKE '%Firefox%' OR rule_json ILIKE '%Mozilla%'
