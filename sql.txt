-- r_ds_outreach_snapshot
SELECT DISTINCT outreach_id
FROM new_email
WHERE subject ILIKE '%Holiday Increased CPA Rates%'
  AND created_at >= '2025-11-07'::timestamp
  AND created_at <  '2025-11-15'::timestamp
ORDER BY outreach_id;


-- r_ds_outreach_snapshot
SELECT outreach_id,
       COUNT(*) AS recipients,
       MIN(created_at) AS first_email,
       MAX(created_at) AS last_email
FROM new_email
WHERE subject ILIKE '%Holiday Increased CPA Rates%'
  AND created_at >= '2025-11-07'::timestamp
  AND created_at <  '2025-11-15'::timestamp
GROUP BY outreach_id
ORDER BY outreach_id;


-- r_ds_outreach_snapshot (schema names as in the vetted index)
SELECT *
FROM outreach.outreach_log
WHERE outreach_id IN (74055, 74060)
ORDER BY outreach_id, created_at;

SELECT *
FROM outreach.icee_job
WHERE outreach_id IN (74055, 74060)
ORDER BY outreach_id;

SELECT outreach_id, COUNT(*) AS preview_count
FROM outreach.segment_preview
WHERE outreach_id IN (74055, 74060)
GROUP BY outreach_id
ORDER BY outreach_id;


-- r_ds_outreach_snapshot
SELECT *
FROM new_email
WHERE outreach_id = 74055
LIMIT 1;
