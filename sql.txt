-- r_ds_tracking
SELECT slug,
       vanity_link_id,
       advertiser_id,
       partner_id,
       destination_url,
       is_deleted,
       last_updated_datetime
  FROM vanity_link
 WHERE vanity_domain = 'your.omnisend.com'
   AND slug IN ('KatieA','KatieArnold','JKoBaw','Z6JqD1','e1JaMj');
