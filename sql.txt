-- DRAFT (r_ds_iracct): locate the partner invoice by invoice number
SELECT *
FROM iracct_recipientgenerated_invoice rgi
WHERE rgi.invoice_number = '170911-3482';

-- DRAFT: pull its line items (table name may be iracct_campaign_recipient_generated_lineitem)
SELECT *
FROM iracct_campaign_recipient_generated_lineitem li
WHERE li.recipient_generated_invoice_id = <id from rgi above>
ORDER BY li.line_number;
