-- DS: r_ds_iraction_sharded
select
  action_date,
  doe, uoe,
  campaign_id,
  oid,
  action_id,
  action_tracker_id,
  ref_type,
  adv_saleamt,
  adv_commission,
  batch_id,
  adj_reason,
  new_status,
  adv_cust_status,
  sku,
  quantity,
  payout_trace
from irac_action2
where oid = '15432204CGB'
and campaign_id = 8089
order by action_date desc
limit 200;
+-------------+-----+-----+-------------+-----+-----------+-------------------+----------+-------------+----------------+----------+------------+------------+-----------------+-----+----------+--------------+
| action_date | doe | uoe | campaign_id | oid | action_id | action_tracker_id | ref_type | adv_saleamt | adv_commission | batch_id | adj_reason | new_status | adv_cust_status | sku | quantity | payout_trace |
+-------------+-----+-----+-------------+-----+-----------+-------------------+----------+-------------+----------------+----------+------------+------------+-----------------+-----+----------+--------------+
+-------------+-----+-----+-------------+-----+-----------+-------------------+----------+-------------+----------------+----------+------------+------------+-----------------+-----+----------+--------------+

-- DS: r_ds_actionlogging_sharded
select *
from ir_referrer
where action_id = '<paste action_id from step 1>'
order by ref_date desc;


-- DS: r_ds_ods
-- payload body of the submission
select payload
from ir_api_log
where batch_id = 'A-<your batch id>';

-- (optional) see server-side error details for that batch
select error_json
from ir_api_log
where batch_id = 'A-<your batch id>';
