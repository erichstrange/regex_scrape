-- BigQuery (datalake_irdwm): Confirm the advertiser exists in this project
SELECT
  advertiser_id,
  advertiser_name,
  network_id,
  status,
  date_from,
  date_to
FROM datalake_irdwm.advertiser_dim
WHERE advertiser_id = 1207187
ORDER BY date_from DESC
LIMIT 25;
