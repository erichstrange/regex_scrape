-- SELECT-35: Grandchild Action Attribution (adapt for this OID)
select action_date,doe,uoe,campaign_id,oid,action_id,action_tracker_id,ref_type,
       adv_saleamt,adv_commission,batch_id,adj_reason,new_status,adv_cust_status,
       sku,quantity,payout_trace
from irac_action2
where oid = '15432204CGB';


-- SELECT-2: detailed click data by campaign/tracker/oid
select *
from ir_referrer
where campaign_id = 8089
  and oid = '15432204CGB'
order by ref_date desc
limit 10;


-- SELECT-1 / Query 3: payload by batch_id (use the A-... value from your screenshot)
select payload
from ir_api_log
where batch_id = 'A-<from-screenshot>';
