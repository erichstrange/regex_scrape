-- Is the tracker there and which program owns it?
SELECT id, name, ircm_campaign_id /* + any status fields available */
FROM ircm_actiontracker
WHERE id = 60393;

-- Are payable events configured for it (e.g., purchase)?
SELECT *
FROM ircm_payable_event
WHERE ircm_actiontracker_id = 60393;
