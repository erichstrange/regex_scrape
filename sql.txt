SELECT
  outreach_id,
  outreach_type,
  COUNT(*) AS recipient_count,
  MIN(created_at) AS first_created_at,
  MAX(created_at) AS last_created_at
FROM new_email
WHERE outreach_id = 74055
GROUP BY outreach_id, outreach_type;



SELECT *
FROM new_email
WHERE outreach_id = 74055
LIMIT 1;
