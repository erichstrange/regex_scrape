SELECT change_date,
       username,
       auditedvariable,
       previousval,
       newval
FROM   m_audit_log_entry
WHERE  core_account_id = 5077455                           -- Wolf & Badger
  AND  objectid IN (3035,3036,3205)                        -- campaign IDs
  AND  objectclassname = 'estalea.ir.creator.domain.CreatorCampaignInvitation'
       /* ⬆ objectclassname sometimes varies; try LIKE '%Creator%Invitation%' if no rows */
ORDER  BY change_date DESC
LIMIT 400;
