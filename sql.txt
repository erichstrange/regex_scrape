-- Data Source selector:  r_ds_ircs
USE DATABASE regcompliance;

SELECT partner_id,
       media_property_id,
       domain,
       status,
       deleted_at
FROM   media_properties_domains
WHERE  domain ILIKE '%pickyourbank.com%';
