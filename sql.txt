-- DRAFT (DataSource: r_ds_outreach_snapshot)
SELECT COUNT(*) AS total_rows
FROM outreach.new_email
WHERE outreach_id = 74055;


-- DRAFT (DataSource: r_ds_outreach_snapshot)
SELECT outreach_type, outreach_id, message_id, email_address, created_at, subject
FROM outreach.new_email
WHERE outreach_id = 74055
LIMIT 50;


-- DRAFT (DataSource: r_ds_outreach_snapshot)
SELECT *
FROM outreach.icee_job
WHERE outreach_id = 74055
ORDER BY created_at DESC
LIMIT 10;

-- DRAFT (DataSource: r_ds_outreach_snapshot)
SELECT *
FROM outreach.outreach_log
WHERE outreach_id = 74055
ORDER BY created_at DESC
LIMIT 100;


-- DRAFT (DataSource: r_ds_outreach_snapshot)
SELECT COUNT(*) AS segment_count
FROM outreach.segment_preview
WHERE outreach_id = 74055;


-- DRAFT (DataSource: r_ds_outreach_snapshot)
-- Inspect the columns first to locate the HTML/body field name.
SELECT *
FROM outreach.new_email
WHERE outreach_id = 74055
LIMIT 1;
