/* All actions where the same SubID (pubsubid1) repeats */
SELECT *
FROM action_fact
WHERE campaign_id = 5714
  AND pubsubid1 IN (
    SELECT pubsubid1
    FROM action_fact
    WHERE campaign_id = 5714
    GROUP BY pubsubid1
    HAVING COUNT(pubsubid1) > 1
  )
ORDER BY pubsubid1, action_datetime;
