select * from ircm_signedcontract
where ircm_campaign_id = 22648


SELECT ins.name AS template_term_name, st.name AS scheduled_term_name, st.start_date, st.end_date
, atr1.name AS tracker_name_1
FROM ircm_scheduled_terms st
INNER JOIN ircm_insertionorder ins on ins.id=st.ircm_insertionorder_id
WHERE st.ircm_campaign_id = 22648
AND st.start_date >= now()
ORDER BY st.start_date DESC


SELECT * FROM iram_account WHERE id = 1450394
