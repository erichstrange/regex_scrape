//* Use this with the 500-ID IN-lists above */
SELECT
  cf.click_id,
  cf.event_datetime,
  cf.publisher_id,
  cf.ref_url,
  cf.landing_page_url
FROM click_fact cf
WHERE cf.click_id IN
(/* paste a sqsp_inlist_500_batch_XX.txt file here */)
AND cf.campaign_id = 9084
-- no ORDER BY (faster)
LIMIT 100000;

