-- DRAFT: verify product entitlements for two users on one IR account
SELECT u.id AS usership_id, ce.email, upa.product_enabled, upa.campaign_json, upa.dlu, upa.ulu
FROM iram_usership u
JOIN core_estaleauser ce ON ce.id = u.account_user_id
LEFT JOIN iram_user_product_access upa ON upa.iram_usership_id = u.id
WHERE u.irAccount_Id = 5399049
  AND ce.email IN ('josh.conyard@exinity.com','line.peteri@exinity.com');
