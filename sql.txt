-- Look for recent API errors for this account
SELECT batch_id, error_json, doe, payload
FROM ir_api_log
WHERE iram_account_id = 6267447
  AND doe >= '2025-06-15'
  AND error_json IS NOT NULL
ORDER BY doe DESC;