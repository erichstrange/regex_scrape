-- Conversion Logs (All Types) — vetted
SELECT event_datetime, advertiser_id, campaign_id, action_tracker_id, method, batch_id, oid, items, currency, gross_amount, json
FROM conversion_fact
WHERE campaign_id IN (19183)
  AND action_tracker_id IN (60393)
  AND event_datetime >= '2025-10-23' AND event_datetime < '2025-10-24';
