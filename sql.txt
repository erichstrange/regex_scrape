-- Which domain(s) are configured on their programs?
SELECT c.id AS campaign_id, c.name,
       it.tracking_domain, it.sub_tracking_domain, it.landing_page
FROM ircm_technicalintegration it
JOIN ircm_campaign c ON c.id = it.ircm_campaign_id
WHERE c.account_id = 86394;
